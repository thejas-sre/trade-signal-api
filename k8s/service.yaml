apiVersion: v1
kind: Service
metadata:
  name: trade-signal-api-service
spec:
  selector:
    app: trade-signal-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
